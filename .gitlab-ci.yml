stages:
    - build
    - deploy

.build:
    stage: build
    artifacts:
        paths: [dist]
    script:
        - ./build.sh

linux:
    tags: [linux]
    extends: .build

apple:
    tags: [macos]
    extends: .build
